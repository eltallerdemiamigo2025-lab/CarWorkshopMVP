---
const navLinks = [
    { href: '/', label: 'Inicio' },
    { href: '/neumaticos', label: 'Neumáticos' },
    { href: '/filtros', label: 'Aceites y Filtros' },
    { href: '/baterias', label: 'Baterías' },
    { href: '/frenos', label: 'Frenos' },
    { href: '/mecanica', label: 'Mecánica' },
];
---
<header>
    <div class="container header-container">
        <a href="/" class="logo">eltaller<span class="highlight">demiamigo</span>.com</a>

        <button class="menu-toggle" aria-label="Abrir menú">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <nav class="nav-menu">
            <ul>
                {navLinks.map(link => (
                    <li><a href={link.href}>{link.label}</a></li>
                ))}
            </ul>
        </nav>
    </div>
</header>

<script is:inline>
    // Script global para el menú hamburguesa que funciona con Astro
    function initMenu() {
        const menuBtn = document.querySelector('.menu-toggle');
        const nav = document.querySelector('.nav-menu');
        
        // Limpiar event listeners anteriores
        if (menuBtn) {
            menuBtn.replaceWith(menuBtn.cloneNode(true));
        }
        
        // Obtener elementos frescos
        const newMenuBtn = document.querySelector('.menu-toggle');
        const newNav = document.querySelector('.nav-menu');
        
        // Reset estado
        if (newNav) newNav.classList.remove('active');
        if (newMenuBtn) newMenuBtn.classList.remove('active');
        
        if (newMenuBtn && newNav) {
            newMenuBtn.addEventListener('click', () => {
                newNav.classList.toggle('active');
                newMenuBtn.classList.toggle('active');
            });
            
            // Cerrar al hacer clic en enlaces
            const links = newNav.querySelectorAll('a');
            links.forEach(link => {
                link.addEventListener('click', () => {
                    newNav.classList.remove('active');
                    newMenuBtn.classList.remove('active');
                });
            });
        }
    }
    
    // Ejecutar al cargar la página
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initMenu);
    } else {
        initMenu();
    }
    
    // Ejecutar en cada navegación de Astro
    document.addEventListener('astro:page-load', initMenu);
    document.addEventListener('astro:after-swap', initMenu);
</script>

<style>
    header {
        background-color: var(--color-surface-base);
        padding: 1.5rem 0; /* Aumentar padding vertical */
        border-bottom: 1px solid var(--color-border);
        position: sticky;
        top: 0;
        z-index: 1000;
    }

    .header-container {
        display: flex;
        justify-content: space-between;
        align-items: center;
        max-width: 1600px; /* Aumentar max-width para más espacio */
        margin: 0 auto; /* Centrar el contenedor */
        padding: 0 1rem; /* Reducir padding horizontal */
    }

    .logo {
        font-weight: 700;
        font-size: 1.7rem; /* Aumentar tamaño de fuente */
        text-decoration: none;
        color: var(--color-text-base);
    }

    .logo .highlight {
        color: var(--color-primary);
    }

    .menu-toggle {
        display: none;
        flex-direction: column;
        background: none;
        border: none;
        cursor: pointer;
        padding: 0.5rem;
        gap: 5px; /* Aumentar espacio */
    }

    .menu-toggle span {
        width: 28px; /* Hacerlo un poco más grande */
        height: 3px;
        background-color: var(--color-text-base);
        transition: all 0.3s ease-in-out;
        border-radius: 2px;
    }

    .menu-toggle.active span:nth-child(1) {
        transform: rotate(45deg) translate(6px, 6px);
    }

    .menu-toggle.active span:nth-child(2) {
        opacity: 0;
    }

    .menu-toggle.active span:nth-child(3) {
        transform: rotate(-45deg) translate(6px, -6px);
    }

    nav {
        display: block;
    }

    nav ul {
        list-style: none;
        display: flex;
        align-items: center; /* Centrar verticalmente */
        gap: 1.5rem; /* Aumentar más el espaciado */
        margin: 0;
        padding: 0;
    }

    nav a {
        text-decoration: none;
        color: var(--color-text-muted);
        font-weight: 600;
        font-size: 1rem; /* Reducir tamaño de fuente */
        transition: color 0.3s, transform 0.3s;
        padding: 0.5rem 0;
        position: relative;
        white-space: nowrap; /* Evitar que el texto se divida */
    }

    nav a::after {
        content: '';
        position: absolute;
        width: 0;
        height: 2px;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        background-color: var(--color-primary);
        transition: width 0.3s ease-in-out;
    }

    nav a:hover {
        color: var(--color-primary);
    }

    nav a:hover::after {
        width: 100%;
    }


    /* Responsive para móviles */
    @media (max-width: 768px) {
        .header-container {
            padding: 0 1.5rem; /* Ajustar padding para móviles */
        }

        .menu-toggle {
            display: flex;
            z-index: 1001; /* Asegurar que esté por encima del menú */
        }

        .nav-menu {
            position: fixed; /* Usar fixed para cubrir toda la pantalla */
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: rgba(24, 24, 27, 0.95); /* Fondo oscuro semi-transparente */
            backdrop-filter: blur(8px);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            transform: translateX(100%); /* Empezar fuera de la pantalla */
            opacity: 0;
            visibility: hidden;
            transition: transform 0.4s cubic-bezier(0.23, 1, 0.32, 1), opacity 0.4s;
        }

        .nav-menu.active {
            transform: translateX(0);
            opacity: 1;
            visibility: visible;
        }

        .nav-menu ul {
            flex-direction: column;
            gap: 2rem; /* Espaciado entre elementos del menú */
            padding: 0;
            text-align: center;
        }

        .nav-menu li {
            border: none;
        }

        .nav-menu a {
            display: block;
            padding: 1rem 2rem;
            color: #FAFAFA; /* Texto claro para contraste */
            font-size: 2rem; /* Letras grandes para el menú móvil */
            font-weight: 700;
            transition: color 0.3s, transform 0.3s;
        }

        .nav-menu a:hover {
            color: var(--color-primary);
            transform: scale(1.1);
        }
    }
</style>