---
import 'boxicons/css/boxicons.min.css';
import Button from '../ui/Button.astro';

// Definimos la estructura de los datos para claridad y autocompletado
interface Step {
  icon: string; // Nombre del icono de Boxicons, ej: 'bx-calendar-check'
  title: string;
  description: string;
}
interface Props {
  steps: Step[];
  guaranteePoints: string[];
}

const { steps, guaranteePoints } = Astro.props;
---

<section class="how-it-works">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">Tu Coche en Buenas Manos, en 3 Simples Pasos</h2>
      <p class="section-subtitle">
        Hemos diseñado un proceso transparente y sencillo para que no tengas que preocuparte por nada. Tu tranquilidad es nuestra prioridad.
      </p>
    </div>

    <div class="steps-container">
      {steps.map((step, index) => (
        <div class="step-card">
          <div class="step-number">{`0${index + 1}`}</div>
          <div class="step-icon-wrapper">
            <i class={`bx ${step.icon}`}></i>
          </div>
          <div class="step-content">
            <h3>{step.title}</h3>
            <p>{step.description}</p>
          </div>
        </div>
      ))}
    </div>

    <div class="guarantee-box">
        <div class="guarantee-icon">
            <i class='bx bxs-shield-check'></i>
        </div>
        <div class="guarantee-content">
            <h4>Nuestra Garantía de Confianza</h4>
            <ul>
                {guaranteePoints.map(point => (
                    <li>
                        <i class='bx bx-check-circle'></i>
                        <span>{point}</span>
                    </li>
                ))}
            </ul>
        </div>
    </div>
    
    <div class="final-cta">
        <Button href="https://wa.me/34000000000?text=Hola,%20he%20visto%20cómo%20funciona%20el%20servicio%20y%20quiero%20pedir%20cita." target="_blank">
            Entendido, ¡Quiero mi Cita!
        </Button>
    </div>
  </div>
</section>

<style>
/* Estilos principales de la sección */
.how-it-works {
    padding: 5rem 0;
    background-color: var(--color-surface-muted);
    position: relative;
}
.section-header { text-align: center; margin-bottom: 4rem; }

/* El contenedor de los pasos con una línea de conexión */
.steps-container {
    position: relative;
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
}

/* Línea de conexión para escritorio */
@media (min-width: 768px) {
    .steps-container {
        grid-template-columns: repeat(3, 1fr);
        gap: 2rem;
    }
    .steps-container::before {
        content: '';
        position: absolute;
        top: 60px; /* Alineado con los iconos */
        left: 15%;
        right: 15%;
        height: 2px;
        background: repeating-linear-gradient(90deg, var(--border-color), var(--border-color) 10px, transparent 10px, transparent 20px);
        z-index: 0;
    }
}

/* Estilo de cada tarjeta de paso */
.step-card {
    background: var(--color-surface-base);
    border-radius: 12px;
    padding: 2.5rem 2rem;
    text-align: center;
    border: 1px solid var(--color-border);
    box-shadow: 0 4px 15px rgba(0,0,0,0.03);
    position: relative;
    z-index: 1;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.step-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.07);
}

.step-number {
    position: absolute;
    top: -1.5rem;
    left: 50%;
    transform: translateX(-50%);
    font-size: 4rem;
    font-weight: 700;
    color: var(--color-border);
    z-index: -1;
}

.step-icon-wrapper {
    width: 80px;
    height: 80px;
    margin: 0 auto 1.5rem auto;
    border-radius: 50%;
    background-color: var(--color-primary);
    display: flex;
    align-items: center;
    justify-content: center;
    border: 6px solid #fff3d4;
}
.step-icon-wrapper i {
    font-size: 2.5rem;
    color: var(--color-text-base);
}
.step-content h3 {
    font-size: 1.5rem;
    margin-bottom: 0.75rem;
}
.step-content p {
    color: var(--color-text-muted);
    font-size: 0.95rem;
}

/* El ancla de confianza */
.guarantee-box {
    margin: 4rem auto 0 auto;
    max-width: 900px;
    background-color: var(--color-surface-base);
    border-radius: 12px;
    border: 1px solid var(--color-border);
    padding: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 1.5rem;
    box-shadow: 0 4px 15px rgba(0,0,0,0.03);
}

@media (min-width: 768px) {
    .guarantee-box { flex-direction: row; text-align: left; }
}

.guarantee-icon {
    flex-shrink: 0;
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background-color: #e8f5e9;
    color: #4caf50;
    display: flex;
    align-items: center;
    justify-content: center;
}
.guarantee-icon i { font-size: 2.5rem; }
.guarantee-content h4 { font-size: 1.25rem; margin-bottom: 1rem; }
.guarantee-content ul { list-style: none; display: grid; gap: 0.75rem; }
@media (min-width: 640px) { .guarantee-content ul { grid-template-columns: repeat(2, 1fr); } }
.guarantee-content li { display: flex; align-items: center; gap: 0.5rem; }
.guarantee-content li i { color: #4caf50; font-size: 1.25rem; }

/* CTA Final */
.final-cta {
    text-align: center;
    margin-top: 3rem;
}
</style>