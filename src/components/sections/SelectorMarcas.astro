---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';
import 'boxicons/css/boxicons.min.css';

// Importamos los logos
const imageAssets = import.meta.glob<{ default: ImageMetadata }>('/src/assets/logos/*.{png,jpg,jpeg,webp}', { eager: true });
const svgAssets = import.meta.glob<string>('/src/assets/logos/*.svg', { as: 'raw', eager: true });

// Definición simplificada de las PROPS
interface Marca { name: string; logo: string; description: string; }
interface Props { 
    marcasPrincipales: Marca[]; 
    marcasConfianza?: Marca[]; // Opcional
    whatsappNumber: string; 
}
const { marcasPrincipales, marcasConfianza = [], whatsappNumber } = Astro.props;

// Función para el enlace de WhatsApp
const generateWhatsAppLink = (brandName: string) => {
    const message = encodeURIComponent(`Hola, estoy interesado/a en neumáticos de la marca ${brandName}. ¿Me podéis asesorar?`);
    return `https://wa.me/${whatsappNumber}?text=${message}`;
}
---

<div class="marcas-container">
    <h3 class="marcas-title">Primeras Marcas</h3>
    
    <div class="marcas-grid">
        {marcasPrincipales.map(marca => (
            <a href={generateWhatsAppLink(marca.name)} target="_blank" class="brand-card">
                <div class="logo-wrapper">
                    {(() => {
                        const imagePath = `/src/assets/logos/${marca.logo}`;
                        // Caso 1: Es un SVG
                        if (marca.logo.endsWith('.svg')) {
                            const rawSvg = svgAssets[imagePath];
                            if (rawSvg) return <div class="logo-svg-wrapper" set:html={rawSvg} />;
                        }
                        // Caso 2: Es una imagen normal (PNG, JPG)
                        else {
                            const image = imageAssets[imagePath]?.default;
                            if (image) return <Image src={image} alt={`Logo ${marca.name}`} class="brand-logo-raster" loading="lazy" />;
                        }
                        // Caso 3: No se encuentra el logo
                        return <div class="logo-placeholder">{marca.name}</div>;
                    })()}
                </div>
                <div class="card-content">
                    <h4>{marca.name}</h4>
                    <p>{marca.description}</p>
                </div>
                <div class="card-action">
                    <span>Consultar por WhatsApp</span>
                    <i class='bx bxl-whatsapp'></i>
                </div>
            </a>
        ))}
    </div>

    {marcasConfianza.length > 0 && (
        <>
            <h3 class="marcas-title marcas-confianza-title">Marcas de Confianza</h3>
            
            <div class="marcas-grid marcas-confianza-grid">
                {marcasConfianza.map(marca => (
                    <a href={generateWhatsAppLink(marca.name)} target="_blank" class="brand-card brand-card-confianza">
                        <div class="logo-wrapper">
                            {(() => {
                                const imagePath = `/src/assets/logos/${marca.logo}`;
                                // Caso 1: Es un SVG
                                if (marca.logo.endsWith('.svg')) {
                                    const rawSvg = svgAssets[imagePath];
                                    if (rawSvg) return <div class="logo-svg-wrapper" set:html={rawSvg} />;
                                }
                                // Caso 2: Es una imagen normal (PNG, JPG)
                                else {
                                    const image = imageAssets[imagePath]?.default;
                                    if (image) return <Image src={image} alt={`Logo ${marca.name}`} class="brand-logo-raster" loading="lazy" />;
                                }
                                // Caso 3: No se encuentra el logo
                                return <div class="logo-placeholder">{marca.name}</div>;
                            })()}
                        </div>
                        <div class="card-content">
                            <h4>{marca.name}</h4>
                            <p>{marca.description}</p>
                        </div>
                        <div class="card-action">
                            <span>Consultar por WhatsApp</span>
                            <i class='bx bxl-whatsapp'></i>
                        </div>
                    </a>
                ))}
            </div>
        </>
    )}
</div>

<style>
    .marcas-container {
        max-width: 1200px;
        margin: 0 auto;
    }
    
    .marcas-title {
        font-size: 2rem;
        font-weight: 700;
        color: var(--color-text-base);
        text-align: center;
        margin-bottom: 3rem;
        position: relative;
    }
    
    .marcas-title::after {
        content: '';
        position: absolute;
        bottom: -0.5rem;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 3px;
        background-color: var(--color-primary);
        border-radius: 2px;
    }
    
    .marcas-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 1.5rem;
    }
    
    .brand-card {
        display: flex;
        flex-direction: column;
        background: var(--color-surface-base);
        border-radius: 16px;
        border: 1px solid var(--border-color);
        overflow: hidden;
        text-decoration: none;
        color: var(--color-text-base);
        box-shadow: 0 4px 20px rgba(0,0,0,0.05);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    
    .brand-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 12px 30px rgba(0,0,0,0.1);
    }
    
    .logo-wrapper {
        height: 200px;
        width: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1.5rem;
        background-color: #fdfdfd;
        transition: all 0.3s ease-in-out;
    }
    
    .brand-card:hover .logo-wrapper {
        transform: scale(1.05);
    }
    
    .logo-svg-wrapper {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 100%;
        height: 100%;
    }
    
    .logo-svg-wrapper :global(svg) {
        height: 95%;
        width: auto;
        max-width: 90%;
        fill: currentColor;
    }
    
    .brand-logo-raster {
        height: 95%;
        width: auto;
        max-width: 90%;
        object-fit: contain;
    }

    /* Make b_autocenter.jpg and b_gti.jpg 20% bigger */
    .brand-logo-raster[src*="b_autocenter.jpg"],
    .brand-logo-raster[src*="b_gti.jpg"] {
        height: 122%;
        max-width: 116%;
    }

    
    .logo-placeholder {
        font-weight: bold;
        color: var(--color-text-muted);
        background-color: #fdfdfd;
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100%;
        width: 100%;
    }
    
    .card-content {
        padding: 1.5rem;
        flex-grow: 1;
    }
    
    .card-content h4 {
        font-size: 1.5rem;
        margin-bottom: 0.5rem;
        font-weight: 600;
    }
    
    .card-content p {
        font-size: 0.95rem;
        color: var(--color-text-muted);
        line-height: 1.6;
    }
    
    .card-action {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
        background-color: var(--color-primary);
        color: var(--color-text-base);
        font-weight: 600;
        transition: background-color 0.3s;
    }
    
    .card-action i {
        font-size: 1.5rem;
        margin-left: 0.5rem;
    }
    
    .brand-card:hover .card-action {
        background-color: var(--color-primary-dark, #e6c43d);
    }
    
    /* Estilos para marcas de confianza */
    .marcas-confianza-title {
        margin-top: 4rem;
        color: var(--color-text-muted);
    }
    
    .marcas-confianza-title::after {
        background-color: var(--color-text-muted);
    }
    
    .brand-card-confianza {
        opacity: 0.9;
    }
    
    .brand-card-confianza:hover {
        opacity: 1;
    }
    
    .brand-card-confianza .card-action {
        background-color: var(--color-text-muted);
        color: white;
    }
    
    .brand-card-confianza:hover .card-action {
        background-color: var(--color-text-base);
    }
    
    /* Responsive Design */
    @media (max-width: 768px) {
        .marcas-container {
            padding: 0 1rem;
        }
        
        .marcas-grid {
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1rem;
        }
        
        .marcas-title {
            font-size: 1.75rem;
            margin-bottom: 2rem;
        }
        
        .card-content {
            padding: 1.25rem;
        }
        
        .card-content h4 {
            font-size: 1.25rem;
        }
        
        .card-content p {
            font-size: 0.9rem;
        }
        
        .card-action {
            padding: 0.75rem;
            font-size: 0.9rem;
        }
        
        .card-action i {
            font-size: 1.2rem;
        }
    }
    
    @media (max-width: 480px) {
        .marcas-container {
            padding: 0 0.5rem;
        }
        
        .marcas-grid {
            grid-template-columns: 1fr;
            gap: 0.75rem;
        }
        
        .marcas-title {
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
        }
        
        .card-content {
            padding: 1rem;
        }
        
        .card-content h4 {
            font-size: 1.2rem;
        }
        
        .card-content p {
            font-size: 0.85rem;
        }
        
        .logo-wrapper {
            height: 130px;
            padding: 1rem;
        }
        
        .card-action {
            padding: 0.75rem;
            font-size: 0.85rem;
        }
        
        .card-action i {
            font-size: 1.1rem;
        }
    }
</style>